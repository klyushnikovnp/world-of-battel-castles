<div class="col-6 col-lg-4 align-self-start align-self-lg-center">  
  {% block content %}
  {% if site_logo %}
  <div class="row align-items-center logo">
		<div class="col-5">
			<a href="{{ path('<front>') }}" rel="home">
			  <img src="{{ site_logo }}" alt="{{ 'Главная'|t }}" />
			</a>
		</div>	
		<div class="col-7 col-md-4">
			<a href="{{ path('<front>') }}" rel="home" class="text-white">Centurion Game Studio</a>	
		</div>	
	</div>	
  {% endif %}
  {% endblock %}
</div>